<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="54dp"
    android:orientation="horizontal"
    android:background="#ffffff"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp"
    android:gravity="center_vertical">

    <!-- Priority Accent Bar (Using FrameLayout instead of View for RemoteViews support) -->
    <FrameLayout
        android:id="@+id/itemPriorityBar"
        android:layout_width="4dp"
        android:layout_height="34dp"
        android:background="#e11d48"
        android:layout_marginEnd="12dp" />

    <!-- Task Details -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/itemTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Task Title"
            android:textColor="#0f172a"
            android:textSize="14sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/itemTime"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="10:48 AM"
            android:textColor="#64748b"
            android:textSize="11sp"
            android:layout_marginTop="2dp" />
    </LinearLayout>

    <!-- Checkbox ImageButton -->
    <ImageButton
        android:id="@+id/itemCheckButton"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:padding="6dp"
        android:background="@android:color/transparent"
        android:src="@drawable/ic_check_box_outline"
        android:contentDescription="Toggle Task" />

</LinearLayout>
